import styled from 'styled-components';

const ManagementPageRow = styled.div`
	position: relative;
	display: flex;
	align-items: center;
	width: clamp(50%, 108rem, 70%);
	margin: 0 auto;
	margin-top: 4.65rem;
	column-gap: 2.85rem;
`;

const S = { ManagementPageRow };

export default S;
