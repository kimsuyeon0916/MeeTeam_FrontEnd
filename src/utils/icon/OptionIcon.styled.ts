import styled from 'styled-components';

const OptionIconLayout = styled.button`
	all: unset;
	display: flex;
	margin-left: auto;
	cursor: pointer;
`;

const S = { OptionIconLayout };

export default S;
