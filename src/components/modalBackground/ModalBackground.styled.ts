import styled from 'styled-components';

const ModalBackground = styled.section`
	position: fixed;
	top: 0;
	left: 0;
	width: 100vw;
	height: 100vh;
	z-index: 105;
	background-color: rgba(0, 0, 0, 0.15);
`;

const S = { ModalBackground };

export default S;
