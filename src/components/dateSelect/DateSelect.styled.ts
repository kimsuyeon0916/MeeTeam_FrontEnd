import styled from 'styled-components';

const DateSelect = styled.div`
	.date-picker {
		width: 15rem;
		padding: 8px;
		border-radius: 8px;
		cursor: pointer;
		border: none;
		margin-top: 5px;
		outline: none;
	}
`;

const S = { DateSelect };

export default S;
