import styled from 'styled-components';

const WorkRegistrationBox = styled.div`
	display: flex;
	flex-direction: column;
`;

const WorkRegistrationTitle = styled.div`
	color: #000;
	font-size: 1.5rem;
	font-style: normal;
	font-weight: 600;
	line-height: 3.5rem; /* 175% */
	letter-spacing: 0.0125rem;
`;

const S = { WorkRegistrationBox, WorkRegistrationTitle };

export default S;
