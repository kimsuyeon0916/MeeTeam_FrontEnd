import styled from 'styled-components';

const Subtitle = styled.span`
	color: #373f41;
	font-size: 1.5rem;
	font-style: normal;
	font-weight: 700;
	line-height: 2.4rem;
	letter-spacing: -0.04rem;
`;

const S = { Subtitle };

export default S;
