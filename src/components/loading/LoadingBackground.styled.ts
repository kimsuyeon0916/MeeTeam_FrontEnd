import styled from 'styled-components';

const LoadingBackground = styled.article`
	display: flex;
	align-items: center;
	justify-content: center;
	height: 100vh;
	width: 100%;
	background-color: rgba(255, 255, 255, 0.8);
`;

const S = { LoadingBackground };

export default S;
